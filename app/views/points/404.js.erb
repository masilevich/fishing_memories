<% if params[:id] %>
    // Clear the marker itself
    clearMarkerById(<%=params[:id]%>);
    show_ajax_message("<%= j(raw(exception.message)) %> I'd better remove that...", 'error');
<%else%>  
    // notify user
    show_ajax_message("<%= j(raw(exception.message)) %>", 'error');
<%end%>

// Close infowindow
closeInfowindow();