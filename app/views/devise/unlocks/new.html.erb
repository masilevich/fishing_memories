<% provide(:title, t('fishing_memories.devise.unlock.title')) %>

<%= semantic_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <%= f.inputs do %>
    <%=f.input :email, label: t('fishing_memories.devise.email.title')%>
  <% end %>

  <%= f.actions do %>
    <%=f.action :submit, label: t('fishing_memories.devise.unlock.submit'), button_html: { value: t('fishing_memories.devise.unlock.submit') } %>
  <% end%>
<% end %>

<%= render "devise/shared/links" %>
