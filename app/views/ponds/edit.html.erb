<%= semantic_form_for(@resource) do |f| %>

	<%= render 'form_fields', f: f %>
	
	<%=f.actions do%>
		<%=f.action :submit, label: t("fishing_memories.update_model", model: resource_label), button_html: { value: t("fishing_memories.update_model", model: resource_label) } %>
	<% end %>
<% end %>